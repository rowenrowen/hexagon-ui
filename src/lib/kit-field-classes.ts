/**
 * Shared form controls inside marketing blocks — radius follows preview lab
 * `--radius-xl` (see globals `.preview-theme-scope` rules).
 */

export const KIT_FIELD_INPUT =
  "min-w-0 flex-1 rounded-[var(--radius-xl)] border border-border bg-background px-4 py-2.5 text-sm text-foreground outline-none ring-ring placeholder:text-muted-foreground focus-visible:ring-2";

export const KIT_FIELD_INPUT_COMPACT = `${KIT_FIELD_INPUT} py-2`;
